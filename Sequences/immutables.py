result = True
another_one = result
print(id(result))
print(id(another_one))

# this returns same address for above 2 objects

result=False
print(id(result

# python will not change the change in first address and instead bound the var to new adress
# last id will be moved to another value
